In package.json => jest => preset: "react-native" || "react-expo"

Init eslint:
    yarn run eslint --init

Run eslint:
    yarn run eslint (file_name or directory)

Init flow:
    flow init
    yarn run flow init

Run flow:
    yarn run flow